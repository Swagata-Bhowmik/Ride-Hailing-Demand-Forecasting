# Ride-Hailing Demand Forecasting - default (lightweight) dependencies.
#
# This is the set the DEPLOYED dashboard and the scheduled GitHub Action install.
# Streamlit Community Cloud automatically installs from this file (`requirements.txt`
# at the repo root) - there is no way to point it at a differently-named file - so
# it is deliberately kept light: the dashboard reads prepared artifacts and fits
# only the fast Holt-Winters model at runtime, so it never needs the heavy
# training/deep-learning packages (tensorflow, prophet, xgboost). Those large
# wheels would exceed the free-tier build/memory limits.
#
# For local model training, the deep-learning models, the notebook, and the test
# suite, install the FULL stack instead:
#
#   pip install -r requirements-full.txt

# --- Data handling ---
pandas>=2.0,<3.0
pyarrow>=14.0,<18.0

# --- Visualization (EDA charts, forecast-vs-actual plots) ---
matplotlib>=3.7,<4.0

# --- Statistical diagnostics + the Holt-Winters model used live in the app ---
statsmodels>=0.14,<0.15

# --- Dashboard framework ---
streamlit>=1.30,<2.0
